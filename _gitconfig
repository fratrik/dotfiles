[user]
	name = Craig Fratrik
	email = cfratrik@gmail.com

[alias]
    st = status
    co = checkout
    br = branch
    p = !CURR=$(git branch | awk '/\\\\*/ {print $2}') && git push origin $CURR

[color]
	diff = auto
	status = auto
	branch = auto

[core]
    editor = vi

[commit]
    template = ~/arc/arcanist/resources/git/commit-template.txt
